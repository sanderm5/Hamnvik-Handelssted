# Sanity CMS (public — eksponert til klient)
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production

# Sanity API token (server-only — for draft mode)
SANITY_API_READ_TOKEN=

# Resend e-post API (server-only — for kontaktskjema)
RESEND_API_KEY=
